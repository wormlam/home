# Shared tokens (local only)

Put real tokens in `.secrets/tokens.env`.

- This file is local-only and ignored by Git.
- Keep file permissions restricted (`chmod 600 .secrets/tokens.env`).
- Rotate tokens periodically.

Use `.secrets/tokens.env.example` as the template.
