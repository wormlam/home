# helloworld

Initial commit.

## Shared tokens for agents

1. Copy `.secrets/tokens.env.example` to `.secrets/tokens.env`.
2. Fill `GH_TOKEN` and `VERCEL_TOKEN`.
3. Run:

   ```bash
   source scripts/load-shared-tokens.sh
   ```
